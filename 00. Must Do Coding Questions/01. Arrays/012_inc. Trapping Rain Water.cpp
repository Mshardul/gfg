#include<iostream>

using namespace std;

void TrapWater(vector<int> vec, int n){
  int l=0, r=0, sum=0;
  while()
}

int main(int argc, char const *argv[]) {
  int t;
  cin>>t;
  
  while(t-->0){
    int n;
    cin>>n;
    
    vector<int> vec(n);
    for(int i=0; i<n; i++)
      cin>>vec[i];
    
    TrapWater(vec, n);
  }
  return 0;
}