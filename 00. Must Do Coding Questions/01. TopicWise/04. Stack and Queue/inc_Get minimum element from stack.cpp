// can be done using a stack of int (that captures min, but not with a single int).

/*
The structure of the class is as follows
class _stack{
stack<int> s;
int minEle;
public :
  int getMin();
  int pop();
  void push(int);
};
*/
/*returns min element from stack*/
int _stack :: getMin(){
  
}
/*returns poped element from stack*/
int _stack ::pop(){
  
}
/*push element x into the stack*/
void _stack::push(int x){
  
}