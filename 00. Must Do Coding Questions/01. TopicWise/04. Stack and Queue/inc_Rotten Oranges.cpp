#include<iostream>

using namespace std;

int ChecTime(vector< vector<int> > vec, int row, int col){
  for(int i=0; i<row; i++){
    for(int j=0; j<col; j++){
      
    }
  }
}

int main(int argc, char const *argv[]) {
  int t;
  cin>>t;
  while(t-->0){
    int r, c;
    cin>>r>>c;
    vector< vector<int> > vec;
    vec.resize(row,vector<int>(col));
    
    ChecTime(vec, row, col);
  }
  return 0;
}