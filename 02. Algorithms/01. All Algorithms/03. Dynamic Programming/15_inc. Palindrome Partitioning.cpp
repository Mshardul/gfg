//similar to rod cutting problem - or matrix multiplication problem

#include<iostream>
#include<vector>

using namespace std;

void pp(string s){
	int n = s.size();
	vector<vector<int> > dp;
	dp.resize(n+1, n+1);
	
}

int main(int argc, char const *argv[]) {
	string s;
	cin>>s;
	
	pp(s);
	
	return 0;
}